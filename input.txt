#include <iostream>
#include <string>

/* Variabile globale */
int globalVar = 10; // Variabila globala corecta
std::string globalStr = 20; // Initializare valida
float pi = 3.14;

/* Functie iterativa */
void iterativeFunction(int n) {
    int localVar = 5; // Variabila locala corecta
    std::string localStr = "Hello, world!"; // Initializare corecta
    // Eliminam redeclaratia invalida
    float pi = 6;

    for (int i = 0; i < n; i++) {
        localVar += i;
        // Comentariu valid
        if (localVar > 50) {
            break;
        }
    }
}

/* Functie recursiva */
int recursiveFunction(int n) {
    if (n <= 1) {
        return n;
    }
    return n + recursiveFunction(n - 1); // Apel recursiv valid
}

int main() {
    int x = 5; // Initializare valida
    std::string s = "123"; // Initializare valida
    float y = 1.234; // Initializare valida

    for (int i = 0; i < 10; i++) {
        x += i; // Operare valida
        if (x > 50) {
            // Iesire din bucla daca x > 50
            break;
        }
    }

    std::cout << "Program completat." << std::endl;
    return 0;
}